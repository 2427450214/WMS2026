spring.application.name=wms_backend

# 环境配置
spring.profiles.active=dev

# 服务器配置
server.port=8080

# 日志配置
logging.level.root=info

# Redis配置
spring.data.redis.host=192.168.206.129
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0
spring.data.redis.timeout=30000

# Spring Session配置
# spring.session.store-type=redis
# spring.session.redis.flush-mode=immediate
# spring.session.redis.namespace=spring:session
# spring.session.timeout=7200
# spring.session.redis.save-mode=on_set_attribute

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/wms2?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=030829
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=org.example.wms_backend.entity
mybatis.configuration.map-underscore-to-camel-case=true

